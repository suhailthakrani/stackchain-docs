---
title: SharedPreferences
description: SharedPreferences storage backend generated by stackchain.
---

## YAML

```yaml
stackchain:
  storage:
    - shared_preferences
```

Generates `shared_prefs.dart` under `lib/core/storage/` for non-sensitive key/value data (flags, locale hints, caches coordinated with `cache_manager.dart`).
