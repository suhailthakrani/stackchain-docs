---
title: Roadmap
description: What's planned next for stackchain.
---

Coming next (shipped only when production-ready):

1. **OpenAPI → feature wiring** — attach `api` models/repos to a feature + DI/routes (schemas → files already shipped in 1.4.0)
2. **Architecture lint rules** — enforce your chosen layout
3. **Token refresh** — real refresh queue on 401
4. **Offline module** — Drift/Isar + cache policy
5. **Deep links** — wired from config
6. **Observability** — Crashlytics / error zones that actually initialize
7. **Golden & integration recipes** — per-feature test patterns
8. **Org brick registry** — share company-approved slices

If it isn't trustworthy in a production review, it doesn't ship.
