---
title: Quick start
description: From flutter create to a running scaffold in a few commands.
---

import { Aside, Steps } from '@astrojs/starlight/components';

<Steps>

1. **Create a Flutter app**

   ```bash
   flutter create my_app
   cd my_app
   ```

2. **Add stackchain**

   ```bash
   dart pub add --dev stackchain
   ```

3. **(Optional) Set your stack**

   Create `stackchain.yaml` at the project root:

   ```yaml
   stackchain:
     preset: production_bloc
     features:
       - splash
       - auth
       - home
   ```

   Skip this to use defaults (Bloc + GoRouter + GetIt).

4. **Scaffold**

   ```bash
   dart run stackchain init
   ```

5. **Run**

   ```bash
   flutter pub get
   flutter run
   ```

   With flavors (default):

   ```bash
   flutter run -t lib/main_dev.dart --dart-define=FLAVOR=dev
   ```

</Steps>

## Add a feature

```bash
dart run stackchain feature auth
```

Creates the feature folder, wires routes + DI, adds tests.

## What you get

```text
lib/
├── app/        # App, theme, router
├── core/       # network, storage, DI, session
├── features/   # one folder per feature
└── main.dart
```

<Aside type="tip">
Preview without writing: `dart run stackchain init --dry-run`
</Aside>

## Next

- [Configuration](/stackchain-docs/getting-started/configuration/) — YAML fields
- [Commands](/stackchain-docs/cli/overview/) — everyday CLI usage
- [Examples](/stackchain-docs/guides/examples/) — copy-paste stacks
