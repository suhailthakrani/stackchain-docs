---
title: How it works
description: The simple pipeline behind stackchain.
---

![How stackchain works](/stackchain-docs/how-it-works.svg)

## 1. You declare a stack

In `stackchain.yaml` (or a preset): architecture, state, routing, DI, network, features.

## 2. Commands generate & wire

| Command | What it does |
| --- | --- |
| `init` | Full app scaffold |
| `feature` | Add a feature + wire routes/DI |
| `sync` | Re-wire routes/DI only |
| `migrate` | Switch stack pieces |
| `upgrade` | Refresh dependencies |

## 3. Only marked regions are rewritten

```dart
// <stackchain:routes>
// …generated…
// </stackchain:routes>
```

Code **outside** markers is never touched. Details: [Your code stays safe](/stackchain-docs/guides/smart-merge/).

## 4. Quality check

Every generate can run a health check (`doctor`). Optional `strict_quality: true` fails on analyze issues.
