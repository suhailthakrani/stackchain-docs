---
title: GetX DI
description: GetX dependency injection and bindings with stackchain.
---

## YAML

```yaml
stackchain:
  di: getx
```

Uses GetX bindings / dependency APIs. Smart-defaulted when `state_management: getx` and `di` is omitted.

Feature slices generate `bindings/` for GetX architectures.
